<h1>Books</h1>

<table border="0" >
  <thead>
  <tr>
    <th>Title</th>
    <th>Body</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  </thead>
  <tbody>
  <% @books.each do |book| %>
  <tr>
    <td><%= book.title %></td>
    <td><%= book.body %></td>
    <td><%= link_to "show", book_path(@list.id) %></td>
    <td><%= link_to "edit", edit_book_path(@list.id) %></td>
    <td><%= "destroy" %></td>
  </tr>
  <% end %>
  </tbody>
</table>

<h2>New book</h2>


