<!--  books GET    /books(.:format)        books#index-->
<h1>Books</h1>

<table border="0" >
  <thead>
  <tr>
    <th>Title</th>
    <th>Body</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  </thead>
  <tbody>
  <!--  @books = Book.all-->
  <% @books.each do |book| %>
  <tr>
    <td><%= book.title %></td>
    <td><%= book.body %></td>
    <td><%= link_to "show", show_book_path(@list.id) %></td>
    <td><%= link_to "edit", edit_book_path(@list.id) %></td>
    <td><%= "destroy" %></td>
  </tr>
  <% end %>
  </tbody>
</table>

<!--    new_book GET    /books/new(.:format)        books#new-->
<!--@book = Book.new-->
<h2>New book</h2>
<%= form_with model: @book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  
  <h4>Body</h4>
  <%= f.text_area :body %>
  
  <%= f.submit 'Create Book' %>
<% end %>
